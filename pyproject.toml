[project]
name = "eth-market-making-backtest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10,<4",
    "polars>=1.24,<1.43",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "pytest>=9.1.1",
]
